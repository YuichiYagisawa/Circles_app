<%= form_for(:micropost, url: microposts_path) do |form| %>

  <div class="field">
    <%= form.label :content %>
    <%= form.text_field :content%>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
